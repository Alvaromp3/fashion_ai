# ---- Required ----
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/fashion_ai
AUTH0_DOMAIN=your-tenant.auth0.com
AUTH0_AUDIENCE=https://fashion-ai-api

# Backend must allow your frontend origin (comma-separated for multiple).
# Local dev: CORS_ORIGINS=http://localhost:3000
# Production: CORS_ORIGINS=https://fashion-ai.pages.dev (or your Pages URL)
CORS_ORIGINS=https://your-project.pages.dev

# ML: main Space (CNN + optional ViT). Use your HF Space URL.
ML_SERVICE_URL=https://YOUR_USER-fashion-ai-ml.hf.space
# Optional: separate ViT Space when ViT is used rarely.
# ML_VIT_SERVICE_URL=https://YOUR_USER-fashion-ai-ml-vit.hf.space

# ---- Optional: Auth0 admin roles ----
# AUTH0_ROLES_CLAIM=https://fashion-ai-api/roles

# ---- Optional: Image storage (use R2 or Cloudinary; R2 has no egress cost) ----
# Cloudflare R2 (recommended for free tier)
# R2_ACCOUNT_ID=your-cloudflare-account-id
# R2_ACCESS_KEY_ID=...
# R2_SECRET_ACCESS_KEY=...
# R2_BUCKET_NAME=your-bucket-name
# R2_PUBLIC_URL=https://pub-xxx.r2.dev
# R2_FOLDER=fashion_ai

# Cloudinary (alternative)
# CLOUDINARY_CLOUD_NAME=...
# CLOUDINARY_API_KEY=...
# CLOUDINARY_API_SECRET=...

# ---- Optional: OpenRouter (Mirror / AI features) ----
# OPENROUTER_API_KEY=...
# OPENROUTER_MODEL=openai/gpt-4o-mini

# ---- Free tier safeguards (optional; defaults keep you under free limits) ----
# PRENDAS_MAX_PER_USER=200
# RATE_LIMIT_API_PER_MIN=80
# RATE_LIMIT_CLASSIFY_PER_MIN=20
# RATE_LIMIT_UPLOAD_PER_MIN=15
# R2 storage cap: block uploads when bucket would exceed this (default 9.5 GB). Needs CLOUDFLARE_API_TOKEN.
# R2_SOFT_LIMIT_BYTES=9500000000
# Cloudflare API token (Account > Account Analytics > Read) to check R2 usage; if unset, R2 cap is not enforced.
# CLOUDFLARE_API_TOKEN=...
