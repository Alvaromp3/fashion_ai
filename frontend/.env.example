# Copy to .env for local dev. For production, set these in Cloudflare Pages → Settings → Environment variables.

# ---- Required (same for local and prod) ----
VITE_AUTH0_DOMAIN=your-tenant.auth0.com
VITE_AUTH0_CLIENT_ID=your-spa-client-id
VITE_AUTH0_AUDIENCE=https://fashion-ai-api

# ---- Local dev (use this when running npm run dev) ----
# Callback URL for Auth0 redirect. Local uses localhost automatically if you leave this empty or set:
VITE_AUTH0_CALLBACK_URL=http://localhost:3000
# Backend: leave empty so frontend proxies /api to localhost:4000
# VITE_API_BASE_URL=

# ---- Production (see .env.production.example for full prod template) ----
# Frontend: https://fashion-ai.pages.dev  |  Backend: https://fashion-ai-backend-c6wd.onrender.com
# Set in Cloudflare Pages or in frontend/.env.production then: npm run cloudflare:pages-env
#   VITE_AUTH0_CALLBACK_URL=https://fashion-ai.pages.dev
#   VITE_API_BASE_URL=https://fashion-ai-backend-c6wd.onrender.com
