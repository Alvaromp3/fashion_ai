# Auth0 (required for login). Create a SPA in Auth0; set callback/logout/origins to this app URL.
VITE_AUTH0_DOMAIN=your-tenant.auth0.com
VITE_AUTH0_CLIENT_ID=your-spa-client-id
VITE_AUTH0_AUDIENCE=https://fashion-ai-api
# Local dev:
# VITE_AUTH0_CALLBACK_URL=http://localhost:3000
# Production (set to your Cloudflare Pages URL, no trailing slash):
# VITE_AUTH0_CALLBACK_URL=https://your-project.pages.dev

# Backend API URL (no trailing slash). Leave unset for local dev (same origin).
# Local: leave commented or empty
# Production:
# VITE_API_BASE_URL=https://fashion-ai-backend.onrender.com
